<h1>Your links</h1>
<ul id="links">
  <% @links.each do |link| %>
  <li>
    <a href="<%= link.url %>" title="<%= link.title %>"><%= link.title %></a>
    Tags: 
    <% link.tags.each do |tag| %>
      <%= tag.name%>
    <% end %>
  </li>
  <% end %>
</ul>
